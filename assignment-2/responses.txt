Responses
1. Data travels across the internet using packets that cannot exceed 1500 megabytes.
The information is contained in a wrapper with a header and a footer. Then, this
tells the computer how to internet the information.
2. The relation between a DNS and an IP is that the DNS or domain name service converts
information from the computer into an IP address that is then used to access and
send information across the web.
3. A cookie is a packet of data sent by an Internet server to a browser, which
is returned by the browser each time it subsequently accesses the same server,
used to identify the user or track their access to the server.
4. The protocol, the resource id, and the server name.
